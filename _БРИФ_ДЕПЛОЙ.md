# Бриф: как сдать работу (деплой) — читалка «Путь Будды»

## Проблема
Твои правки **не появились на боевой ссылке**. Факт по репозиторию `brownbrown1978-source/nyondro`:
- ветка одна — `main`; последний коммит — **b27**;
- ни твоей ветки, ни нового файла/зипа b28 нет.
Пользователь видит старую версию — менять нечему. Работа в твоей песочнице **не считается сделанной**.

## ВАЖНО: база разошлась
Репо-версия `ngondro.html` сейчас **b27 (927507 б)**, а твой импорт был **927388 б** — то есть после тебя в `main` уже коммитили (мои фиксы). Если зальёшь b28 поверх своей старой базы — **затрёшь** их.
→ **Переимпортируй актуальный `ngondro.html` из `main` (b27)** и наложи свои b28-правки заново.

В b27 уже сделано (НЕ потерять):
- навигатор раздублирован (меню снова раскрывается);
- линии `.sec-rule` у лотоса на стыках практик убраны;
- именные списки `.lineage` центрируются по содержимому;
- вычищены двойные пробелы в прозе/именах;
- исправлены опечатки нумерации линий (4095: 66→61; 4325: 51→31, 38→56).

## Что значит «сделано»
Изменения **залиты в `brownbrown1978-source/nyondro` → `main`** и видны на Pages:
`https://brownbrown1978-source.github.io/nyondro/ngondro.html`

## Сделай одно из двух
1. **Запушь сам** в `main`. Токен — в `.github-token` (в папке проекта, **не коммить его**). Обязательно бампни версию (иначе кэш не обновится):
   - `sw.js` → `const CACHE = 'ngondro-2026-…-bNN'`;
   - `ngondro.html` → `.build-stamp` («версия 2026-…·bNN»).
2. **Или отдай файл**: полный обновлённый `ngondro.html` (+ `sw.js`) одним зипом — как было с ревью b26. Зальют сам.

## Условия
- Только **single-file**; правки — **якорные literal-замены**, без пакетных/многострочных regex.
- Проверяй результат **на живой ссылке** (жёсткий релоад / очистка SW и кэша), не у себя в песочнице.

## В ответе пришли
- номер версии (`bNN`);
- подтверждение, что Pages отдаёт её (build-stamp на странице совпадает).

Пока этого нет — на ссылке остаётся `b27`.
